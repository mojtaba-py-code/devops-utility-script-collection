# Runtime dependencies
psutil>=7.2.2        # system, process and disk metrics
PyYAML>=6.0.3        # layered YAML configuration
rich>=15.0.0         # pretty CLI output and tables
colorama>=0.4.6      # coloured console logging on Windows
requests>=2.34.2     # HTTP health checks and notifications

# Optional integrations — install as needed for the matching commands:
#   docker>=7.0.0    # `docker` command (Docker SDK for Python)
#   paramiko>=3.4.0  # `ssh` automation over SSH/SFTP
#   reportlab>=4.0.0 # PDF reports (`--report pdf`)
