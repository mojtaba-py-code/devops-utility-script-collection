# Development & test dependencies
-r requirements.txt

pytest>=9.1.1
pytest-cov>=7.1.0
docker>=7.0.0        # exercised via mocks; installed so imports resolve
paramiko>=3.4.0      # exercised via mocks; installed so imports resolve
