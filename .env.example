# Copy this file to `.env` and fill in real values. `.env` is git-ignored and
# is loaded automatically at start-up. NEVER commit real secrets.

# --- SSH / deployment credentials -------------------------------------------
# Referenced by name from config/servers.yaml (password_env / key_passphrase_env).
DB01_SSH_PASSWORD=
WEB01_KEY_PASSPHRASE=

# --- Notifications ----------------------------------------------------------
SLACK_WEBHOOK_URL=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
